Logging:
Need to log dates

Searching:
Only rescan DB when notified of changes instead of polling
Need to filter by dates
Need to filter by band

Server:
Need to set up IP broadcasting
Need to set up missing packet detection
